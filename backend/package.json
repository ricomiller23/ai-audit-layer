{
    "name": "ai-audit-layer-backend",
    "version": "1.0.0",
    "description": "AI Audit Layer - Compliance Dashboard Backend",
    "scripts": {
        "dev": "uvicorn main:app --reload --host 0.0.0.0 --port 8000",
        "start": "uvicorn main:app --host 0.0.0.0 --port 8000",
        "test": "pytest tests/ -v",
        "lint": "ruff check .",
        "format": "black ."
    }
}